{
  "name": "prediction-arena",
  "version": "1.0.0",
  "description": "A self-hostable football match prediction game for friend groups (FIFA / World Cup style).",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  },
  "private": true
}
